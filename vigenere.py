def encrypt_vigenere(plaintext, keyword):
    """
    Encrypts plaintext using the Vigenère cipher with a given keyword.
    
    Parameters:
    - plaintext: str, the original message to encrypt.
    - keyword: str, the keyword used for encryption.
    
    Returns:
    - str: The encrypted message.
    """

    encrypted_text = "Write your code here"
    return encrypted_text

def decrypt_vigenere(ciphertext, keyword):
    """
    Decrypts ciphertext using the Vigenère cipher with a given keyword.
    
    Parameters:
    - ciphertext: str, the encrypted message to decrypt.
    - keyword: str, the keyword used for decryption.
    
    Returns:
    - str: The decrypted message.
    """

    decrypted_text = "Write your code here"
    return decrypted_text
